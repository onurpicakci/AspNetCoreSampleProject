@using EntityLayer.Concrete
@model List<Experience>

@{
    ViewBag.Title = "title";
    Layout = "~/Views/Shared/_NewAdminLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <a href="/Experience/AddExperience" class="btn btn-success float-right">New Experience</a>
            <h4 class="card-title">Experience</h4>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                    <tr>
                        <th>#</th>
                        <th>Title</th>
                        <th>Date</th>
                        <th>Delete</th>
                        <th>Update</th>
                    </tr>
                    </thead>
                    <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.ExperienceId</td>
                            <td>@item.Name</td>
                            <td>@item.Date</td>
                            <td>
                                <a href="/Experience/DeleteExperience/@item.ExperienceId" class="btn btn-outline-danger">Delete</a>
                            </td>
                            <td>
                                <a href="/Experience/UpdateExperience/@item.ExperienceId" class="btn btn-outline-success">Update</a>
                            </td>
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>